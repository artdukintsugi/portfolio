import Image from "next/image";

import MainCard from "./MainCard";

export default function Skills() {
  return (
    <MainCard
      title="Skills"
      paragraph="What am I capable of."
      buttonTitle="Get mindblown (sort of)"
      id="skills"
    ></MainCard>
  );
}
